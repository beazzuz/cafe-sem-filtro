import * as React from "react"
import Layout from "../components/Layout"
import Sobre from "../components/Sobre"

const SobrePage: React.FC = () => {
  return (
    <Layout variant="sobre">
      <Sobre />
    </Layout>
  )
}

export default SobrePage
